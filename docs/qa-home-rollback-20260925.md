# Возврат главной к версии до переделки

По прямому запросу владельца главная возвращена к `17f9349`. Это отменяет первый экран с сгенерированным портретом и сетку карточек из `e1c2ba8`. Портфолио, оригинальное фото на странице «О нас», контакты и меню не откатываются.

## Проверено локально

- `index.html` побайтно совпадает с `17f9349:index.html` после нормализации только трёх версий CSS/JS.
- Содержимое `about.html`, `services.html`, `portfolio.html`, `contact.html`, `src/portfolio-main.html` не изменилось.
- `npm run build`, `npm run check` и `git diff --check` проходят. Проверены 13 страниц, 505 локальных ссылок и ресурсов, JSON-LD, тесты загрузчика.
- На 390 x 844 нет горизонтального переполнения. Фото на главной отсутствует; сцена имеет состояние `complete`, canvas 390 x 812 и видимый ноутбук. Карусель переключается кнопкой с 01 на 02.
- На 1440 x 900 четыре карточки имеют `position: sticky` и прежние отступы 100, 116, 132, 148 px. Скриншот подтверждает наложение первой и второй карточек. Ошибок JavaScript не обнаружено.
- Восстановленные исходники сцены, загрузчика и карточек не перепроектировались; новый код галерей портфолио сохранён.

## Публикация и откат

- Перед изменениями: GitHub `9da7636`; база восстановленной главной: `17f9349`.
- Серверный архив: `/root/backups/mentorios-site-before-home-rollback-20260925.tar.gz`.
- Локальная история: `/tmp/mentorios-before-home-rollback-20260925.bundle`.
- Публикуются только `assets/hero/bundle/air.js`, `main.min.js`, `site-refresh.css` и `index.html`, без удаления файлов.
- На главной версии ресурсов изменены на `20260925-rollback1` и `57`, чтобы не оставался кэш неудачной переделки.
- DNS, TLS, Nginx и рабочая CRM не меняются. Проверка размеров в браузере не заменяет тест физического iPhone или доступности из российских сетей.
