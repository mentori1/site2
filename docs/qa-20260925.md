# Проверка обновления 25 сентября

Главная этой версии впоследствии отклонена владельцем и возвращена к `17f9349`. Проверки ниже являются историческими для `e1c2ba8`, а не описанием текущей главной. Текущая проверка: `docs/qa-home-rollback-20260925.md`. Портфолио и остальные страницы сохранены.

## Проверено локально

- `npm run check`: 13 страниц, 504 локальные ссылки и ресурса, JSON-LD, контакты, порядок и состав портфолио.
- `git diff --check`: без ошибок пробелов.
- Главная 1440 x 900: изображение загружено, горизонтального переполнения нет, середина навигации на x=720. Нет canvas, загрузчика и подключения старой сцены.
- Главная 390 x 844: портрет, ноутбук, обе ссылки и три шага видимы; горизонтального переполнения нет.
- Четыре карточки имеют `position: relative`, `opacity: 1`, не накладываются. На телефоне ручной горизонтальный свайп переводит ко второй карточке без автопроигрывания.
- Основные страницы `/`, `/services`, `/about`, `/contact`, `/blog`, `/portfolio` при ширине 320: scrollWidth=320, потерянных загруженных изображений нет.
- Галерея Mentori: кнопки «Финансы» и «Клиенты» переводят к нужному снимку и обновляют активное состояние. Проверены 320, 390 и 1440px.
- Стеклянное меню сохранено с тремя контактными иконками.
- Замки: переход ко второму и третьему этапам работает; SVG overflow остаётся visible. Исходная исправленная геометрия не менялась.
- Копия CRM изолирована от рабочих данных. Снимки содержат только вымышленные компании и суммы.
- Реальная корзина «Спектра» снята без отправки заказа, тестовый выбор очищен.

## Ограничения

Эмуляция размеров в браузере не является тестом физического iPhone/Safari. Эта проверка не подтверждает доступность сайта через российских провайдеров. Новый Lighthouse не запускался; исторические оценки в README не относятся к этому обновлению.

## Опубликованная версия

Изменения `e1c2ba8` опубликованы в GitHub main и на VPS. Проверены по HTTPS 27 ресурсов: 13 страниц по чистым URL, CSS, JS, sitemap, новый портрет, оригинальное фото, PDF студии и все новые снимки. Каждый вернул HTTP 200; SHA-256 каждого ответа совпал с локальной сборкой. На публичной главной загружен новый портрет, загрузчика и canvas нет. Настройки сети не менялись.

## Откат

Версия до обновления: `17f9349`. Серверный архив: `/root/backups/mentorios-site-before-static-20260925.tar.gz` (14 MiB). DNS, TLS и Nginx не менялись.
