# MENTORI TECHNOLOGIES

Официальный многостраничный сайт компании MENTORI TECHNOLOGIES: системы, автоматизация, сайты, Яндекс Директ и SEO для бизнеса.

- Сайт: https://mentorios.tech/
- Репозиторий: `mentori1/site2`
- Рабочая ветка: `main`
- Telegram: `@Mentori_Re`
- MAX: `https://max.ru/u/f9LHodD0cOKBPM0dbjrb5ezQXBmCYH_aXZDfeNXg5-SMxcV_laJVpwlyBO0`
- Email: `mentraos@mail.ru`
- Телефон: `+7 911 437-85-85`
- Яндекс Метрика: `111247225`
- Обновлено: `25 сентября 2026`

## Обновление 25 сентября

По уточнённому запросу владельца первый экран возвращён к исходной версии `b3b5a9e` от 21 сентября, до всех экспериментов с ноутбуком и портретом. Заголовок: «Excel угробит развитие вашего бизнеса», справа нарисованная HTML-панель клиента с показателями. Разметка `#hero` совпадает с исходной версией. MacBook, фотография и WebGL больше не подключаются. Оригинальная фотография в разделе «О нас» не менялась.

Карточки задач снова накладываются при прокрутке на компьютере; на телефоне возвращена прежняя управляемая карусель. Сетка 2 x 2 и последняя горизонтальная лента удалены. GSAP/ScrollTrigger сохранены для прежнего поведения карточек. Three.js и сцена ноутбука не собираются и не загружаются.

На всех страницах в закреплённой стеклянной шапке оставлены только логотип и кнопка меню справа. Дублирующие ссылки разделов и контактные кнопки удалены из шапки. Разделы, Telegram, MAX и звонок доступны внутри меню; контакты в футере сохранены. Замки трёх этапов сохраняют исправленный viewBox `-12 -24 88 100` и не обрезаются при открытии.

Порядок портфолио: танцевальная студия, «Спектр Металла», собственный продукт Mentori. У «Спектра» показаны главная, каталог с фильтрами и корзина заявки на телефоне. Выбор товаров проверен без отправки заявки, тестовая корзина очищена. У Mentori четыре экрана: проекты, дашборд, финансы, клиенты, а также два мобильных снимка. Галереи переключаются кнопками и нативным горизонтальным скроллом; изображения открываются отдельно в полном размере. Насыщенность и контраст превью усилены умеренно в CSS.

Снимки Mentori сняты с изолированной копии настоящей разметки и компонентов CRM. Все компании и суммы вымышлены. Доступ к сети в копии запрещён через CSP `connect-src 'none'`; auth, cloud-sync, seed и рабочая база не копируются. Демонстрационный сценарий в тексте явно обозначен. Рабочая CRM не изменялась. Для студии сохранены ранее подтверждённые сведения: полтора месяца, самостоятельная запись через Telegram, проверка чеков преподавателем.

### Сборка и проверка

```bash
npm ci
node tools/integrate-hero.mjs
npm run build
npm run check
npm run dev
```

Предпросмотр: http://127.0.0.1:4322/. Чистые пути поддерживает `vite.config.mjs`.
Актуальные шаблоны: `src/hero.html`, `src/portfolio-main.html`; общие стили: `site-refresh.css`; логика: `main.js`. Все страницы подключают `site-refresh.css?v=20260925-excel`. JS главной имеет версию `57`, остальных страниц `56`. Интеграция сохраняет исходный HTML-дашборд, упрощённую шапку и не возвращает ноутбук. Галереи портфолио не менялись.

`npm run check` проверяет 13 страниц, ссылки и ресурсы, JSON-LD, меню и контакты, точный заголовок про Excel, наличие HTML-дашборда, отсутствие MacBook/canvas, scroll-stack, порядок кейсов и состав портфолио. Отдельные тесты загрузчика проверяют таймаут, Escape, повторный визит, уменьшение движения и недоступное хранилище. Браузерную проверку выполнять на ширинах 1440, 390 и 320px. Это не тест физического iPhone или доступности из российских сетей.

`tools/build-portfolio-demo.mjs` создаёт только локальные страницы захвата из исходников CRM; `tools/portfolio-demo-fixture.js` содержит вымышленные данные. Для воспроизведения нужны локальная папка CRM и базовая изолированная копия дашборда. Пути переопределяются через `CRM_SOURCE` и `DEMO_OUTPUT`. Эти страницы и инструменты не публикуются на веб-сервере.

### Источники и публикация

- Главная: исходная разметка HTML-дашборда в `src/hero.html` из `b3b5a9e`.
- Оригинальный портрет: `assets/danila-rodionov-photo-20260924.jpg`.
- Снимки CRM и каталога: `assets/portfolio/*20260925.jpg`.
- Студия: презентация `CRM_MiniApp_STUDII_v6.pptx`, слайды 3 и 6; готовый PDF v6, не база студии.
- Неиспользуемый портрет с MacBook сохранён как ресурс истории, но не подключается к страницам. Происхождение: `docs/visuals-20260925.md`.

Перед восстановлением исходного экрана сохранены `/root/backups/mentorios-site-before-excel-home-20260925.tar.gz` и `/tmp/mentorios-before-excel-home-20260925.bundle`. Источник главной: `b3b5a9e`, не `17f9349` (там уже был MacBook). Содержимое остальных страниц и новые галереи портфолио сохранены; меняется только их общая шапка. Публиковать ресурсы первыми, HTML последним, без `rsync --delete`. Не выгружать исходники CRM, инструменты, `node_modules` и `.git`. DNS, TLS и Nginx не меняются.

## Источник истины

Актуальная версия всегда находится в GitHub, в репозитории `mentori1/site2`, ветка `main`.
Старые локальные папки сайта не использовать и не переносить из них файлы. Для каждой новой работы нужно делать свежий клон GitHub или сначала обновлять существующий чистый клон.

```bash
git clone git@github.com:mentori1/site2.git
cd site2
git switch main
git pull --ff-only origin main
```

Перед изменениями обязательно проверить `git status`. После публикации проверить новый SHA в `origin/main` и живую страницу на `https://mentorios.tech/`.

## Локальный просмотр

Это статический сайт на HTML, CSS и JavaScript. На главной исходный HTML-дашборд и предыдущая анимация карточек. Исходники остаются читаемыми, опубликованные файлы отделены от них. Скролл страницы нативный; WebGL на главной не используется.

```bash
npm run dev
```

Открыть: http://127.0.0.1:4322/. Обычный файловый сервер без настройки маршрутов не поддерживает чистые адреса страниц.

## Страницы

| Файл | Публичный адрес | Назначение |
|---|---|---|
| `index.html` | `/` | Главная страница |
| `portfolio.html` | `/portfolio` | Mentori, танцевальная студия и Спектр Металла |
| `services.html` | `/services` | Услуги и направления работы |
| `about.html` | `/about` | О компании и основателе |
| `contact.html` | `/contact` | Прямые контакты: Telegram, MAX, телефон и почта |
| `blog.html` | `/blog` | Все статьи |
| `blog-yandex-direct-dlya-biznesa.html` | `/blog-yandex-direct-dlya-biznesa` | Статья о Яндекс Директе |
| `blog-seo-prodvizhenie-saita.html` | `/blog-seo-prodvizhenie-saita` | Статья о SEO |
| `blog-crm-dlya-stroitelstva.html` | `/blog-crm-dlya-stroitelstva` | Статья для строительства |
| `blog-crm-dlya-repetitora.html` | `/blog-crm-dlya-repetitora` | Статья для репетиторов |
| `offer.html` | `/offer` | Договор оферты |
| `privacy.html` | `/privacy` | Политика конфиденциальности |
| `consent.html` | `/consent` | Согласие на обработку персональных данных |
| `yandex_dadaa969d58e5279.html` | `/yandex_dadaa969d58e5279.html` | Файл подтверждения Яндекс Вебмастера |

Общие ресурсы: `styles.css` и `main.js` как исходники, `styles.min.css` и `main.min.js` для публикации, облегчённый `home.min.css` для главной, `assets/`, `robots.txt`, `sitemap.xml`, `llms.txt`, `CNAME`.

## Дизайн и темы

Сохранять текущую визуальную систему: тёплый чёрный фон, фирменный оранжевый, Fraunces, Geist и Geist Mono, стеклянные поверхности, плавные анимации и существующую композицию.

Новая версия использует тёмную тему. `data-theme-lock="dark"` отключает реакцию на системную тему; прежний выбор `mentra-theme` больше не влияет на отображение. Старые CSS-правила светлой темы сохранены только как неактивные стили, переключателя в интерфейсе нет.

Не использовать старые изображения с неверной последней буквой. В шапке и футере название `MENTORI` и подписи `TECH` / `TECHNOLOGIES` набраны редактируемым HTML-текстом. PNG логотипа сохранены для разметки Organization и совместимости. Для превью ссылки используется `assets/mentori-technologies-preview-2026.png`.

В шапке используется логотип MENTORI с небольшой подписью TECH справа.

Интерактивная WebGL-сетка Vanta с первого экрана удалена. Фирменные фоновые засветки сохранены без реакции на курсор. На странице «О нас» линия истории заполняется оранжевым по мере прокрутки и последовательно активирует точки. В карточках блога нет клетчатой подложки, переход в статью подписан словом «Читать».

Главное позиционирование строится вокруг отказа от десятков Excel-файлов и ручного переноса данных. Заголовок главной: «Excel угробит развитие вашего бизнеса». После первого экрана идёт прежний блок результата внедрения. Проекты размещены только на отдельной странице портфолио. Страница «Что делаем» объясняет продукт через пять модулей одной системы: перенос данных, заявки и клиентская база, заказы и оплаты, производство со складом и доставкой, интеграции и автоматические действия. После модулей остаются факторы стоимости, дополнительные услуги и FAQ. Отраслевой перечень и отдельная карусель этапов запуска удалены.

Основной CTA «Консультация» ведёт на `/contact`; «Что войдёт в систему» ведёт на `/services`. Отдельный номер под кнопками не показывается. В шапке только логотип и меню; Telegram, MAX и звонок находятся внутри меню.

На странице «Контакты» нет формы заявки. Оставлены четыре прямых способа связи: Telegram `@Mentori_Re`, MAX, телефон `+7 911 437-85-85` и почта `mentraos@mail.ru`.

Для индивидуальной системы числовая цена не публикуется: стоимость определяется после диагностики по составу модулей, ролям, данным и интеграциям. Действующие цены дополнительных услуг сохраняются: сайты от 30 000 ₽, Яндекс Директ от 20 000 ₽, SEO от 30 000 ₽.

В футере телефон, почта, Telegram и MAX показаны компактными иконками с доступными подписями. Иконки вставлены обычными `img` с абсолютными путями `/assets/icons/...`; CSS дополнительно рисует тот же знак как резервный фон для старого HTML из браузерного кэша. Для Telegram и MAX используются официальные фирменные знаки из [пресс-материалов Telegram](https://telegram.org/press) и [брендбука MAX](https://go.max.ru/brandbook), для телефона и почты — цельные SVG без CSS-масок. Декоративные плашки с мигающей точкой удалены с главной, страниц «Что делаем», «О нас» и блога.

## Production-ресурсы

Главная подключает `home.min.css?v=78` из коммита `16abbcb`, остальные страницы подключают `styles.min.css?v=79` из того же коммита. CSS загружается через jsDelivr с привязкой к точному коммиту, чтобы исключить выдачу старой версии из кэша ветки `main`. Это обход ограничений российских провайдеров, которые обрывают крупные ответы Cloudflare примерно после первых 16 КиБ. Относительные ссылки внутри CSS автоматически загружают шрифты через тот же маршрут. Все страницы подключают локальный `main.min.js`: главная с версией `57`, остальные с `56`. Исходники `styles.css` и `main.js` напрямую в HTML не используются.

Описание ограничения со стороны Cloudflare: https://blog.cloudflare.com/russian-internet-users-are-unable-to-access-the-open-internet/

После изменения исходников заново собрать production-файлы и поднять номера версий во всех HTML-файлах. Если менялся CSS, сначала опубликовать коммит со свежими `home.min.css` и `styles.min.css`, затем указать его короткий SHA в jsDelivr URL и опубликовать второй коммит с HTML. Главный CSS очищается по фактически используемой разметке `index.html` и классам из `main.js`.

```bash
mkdir -p /tmp/mentorios-build-home
npx --yes purgecss@7.0.2 --css styles.css --content index.html main.js --output /tmp/mentorios-build-home
npx --yes clean-css-cli@5.6.3 -O2 -o home.min.css /tmp/mentorios-build-home/styles.css
npx --yes clean-css-cli@5.6.3 -O2 -o styles.min.css styles.css
npx --yes terser@5.44.0 main.js --compress --mangle --output main.min.js
```

Проверка:

```bash
rg -n 'main\.min\.js\?v=|(?:home|styles)\.min\.css\?v=' *.html
```

## Производительность

Оценки ниже относятся к предыдущей версии до замены первого экрана. Они не являются замером текущей версии от 25 сентября.

Базовый мобильный отчёт PageSpeed от 2 сентября 2026: производительность `73`, FCP `3,0 с`, LCP `4,9 с`, TBT `0 мс`, CLS `0`, Speed Index `5,4 с`. Полевых данных в отчёте ещё нет.

Контрольный локальный Lighthouse 13.4.1 после оптимизации: производительность `92`, доступность `100`, рекомендации `100`, SEO `100`; FCP `2,1 с`, LCP `2,9 с`, TBT `0 мс`, CLS `0,001`, Speed Index `2,6 с`. Итоговый PageSpeed на публичном домене проверять после обновления Cloudflare и GitHub Pages: сетевые значения могут немного отличаться.

Публичный мобильный PageSpeed после публикации: производительность `96`, доступность `100`, рекомендации `100`, SEO `100`, агентный просмотр `3/3`; FCP `1,7 с`, LCP `2,0 с`, TBT `0 мс`, CLS `0`, Speed Index `4,3 с`. Отчёт: https://pagespeed.web.dev/analysis/https-mentorios-tech/yis38w5hmn?form_factor=mobile

Для ускорения первого экрана:

- заголовок hero больше не ждёт reveal-анимацию;
- главная загружает отдельный CSS объёмом около `15,6 КиБ` в gzip вместо общего файла около `46,7 КиБ`;
- Google Fonts размещены локально в `assets/fonts/`, внешний блокирующий запрос удалён;
- GSAP и ScrollTrigger исключены из загрузки;
- синтетическая прокрутка Lenis удалена: колесо, трекпад и сенсор используют нативный скролл без смены режима после загрузки;
- постоянное курсорное свечение и непрерывный мобильный 3D-цикл удалены, а scroll-обновления hero и cinema объединены по одному на кадр;
- на странице «Что делаем» удалены мобильные горизонтальные карусели, отраслевой блок и отдельный блок этапов запуска; пять модулей идут обычным вертикальным потоком;
- на телефоне у фиксированной шапки и карточек модулей отключён тяжёлый `backdrop-filter`;
- в Safari отключены постоянное SVG-зерно, фоновые анимации и `backdrop-filter`; параллакс, 3D-наклон и GSAP там не запускаются, чтобы нативная прокрутка не вызывала лишнюю перерисовку страницы;
- загрузочная заставка и 3D-модель не скачиваются;
- production CSS и JavaScript минифицированы.

Базовые CSS и шрифты доставляются через jsDelivr. `site-refresh.css` и материалы портфолио загружаются с `mentorios.tech`. HTTPS обслуживается Nginx с сертификатом Let's Encrypt на VPS; Cloudflare используется для DNS без проксирования. HTML-страницы отдаются с `Cache-Control: no-cache, must-revalidate`, поэтому браузер перепроверяет актуальную разметку после каждого обновления; версионированные статические ресурсы продолжают кэшироваться.

## Аналитика

Яндекс Метрика `111247225` загружается динамически только после принятия cookie-баннера. До согласия запрос к `mc.yandex.ru` не выполняется. Факт принятия хранится в `localStorage` под ключом `mentori_cookie_consent_v1` и содержит уникальный ID, дату и способ принятия.

Согласием считаются кнопка «Хорошо», взаимодействие с элементом сайта или прокрутка после 5 секунд. Переход по ссылке политики внутри баннера согласие не создаёт.

UTM-параметры и `yclid` хранятся до конца визита в `sessionStorage` под ключом `mentori_visit_attribution_v1`. Формы заявки на сайте нет, поэтому эти параметры используются только в рамках текущего визита и аналитики переходов. Отдельного серверного журнала согласий нет.

`main.js` отправляет следующие JavaScript-события:

| Идентификатор цели | Событие |
|---|---|
| `click_telegram` | Переход в Telegram |
| `click_phone` | Нажатие на телефон |
| `click_email` | Нажатие на email |
| `cta_contact` | Нажатие кнопки «Консультация», другого CTA контакта или ссылки MAX |
| `blog_article_open` | Открытие статьи из блога |
| `article_read_50` | Прочитано 50% статьи |
| `article_read_90` | Прочитано 90% статьи |
| `article_cta` | CTA внутри статьи |
| `service_click` | Выбор услуги |

В Яндекс Метрике цели из таблицы созданы с типом `JavaScript-событие` и указанными идентификаторами. Автоцели Метрики работают дополнительно и не заменяют эти события.

## Индексация

- Основной домен: `https://mentorios.tech/`
- `robots.txt` разрешает обход сайта и указывает `sitemap.xml`.
- `sitemap.xml` содержит все публичные страницы и статьи.
- `llms.txt` описывает компанию, услуги, материалы и контакты для поисковых и AI-систем.
- Сайт подтверждён в Яндекс Вебмастере.
- Обход страниц по счётчику Метрики `111247225` включён.
- Публичные страницы используют чистые адреса без `.html`; старые адреса перенаправляются кодом `301`.
- Файл подтверждения Яндекс Вебмастера `yandex_dadaa969d58e5279.html` не перенаправляется.

После публикации новой статьи или важного изменения:

1. Обновить `lastmod` страницы в `sitemap.xml`.
2. Проверить, что страница открывается с кодом `200` по HTTPS.
3. Проверить canonical, title, description, H1 и внутренние ссылки.
4. Отправить URL на переобход в Яндекс Вебмастере.
5. Убедиться, что актуальный `sitemap.xml` принят Вебмастером.

## Проверка перед публикацией

```bash
git status --short
rg -n 'Mento_ri|main\.js\?v=|styles\.css\?v=' --glob '*.html'
rg -n '111247225|mentori_cookie_consent_v1' main.js README.md
npm run dev
```

Проверить главную, портфолио, услуги, контакты, блог и все статьи на desktop и mobile. Сайт остаётся тёмным независимо от системной темы. Отдельно проверить cookie-баннер в чистом профиле, отсутствие Метрики до согласия, все три способа согласия, исключение ссылки политики, сохранение UTM/yclid на время визита, ссылки Telegram и MAX, телефон, почту, отсутствие горизонтального переполнения и появление событий в режиме отладки Метрики.

На главной дополнительно проверить плавность прокрутки блока «Производство растёт», обычную сетку четырёх задач на компьютере и нативное горизонтальное перелистывание на телефоне. Карточки не должны закрепляться, пересекаться или становиться полупрозрачными.

## Публикация

GitHub `main` является источником истины. Рабочий сайт размещён на VPS `5.181.109.125` в `/var/www/mentorios-site`; Nginx-конфигурация хранится в репозитории как `nginx/mentorios-site2.conf` и на сервере как `/etc/nginx/sites-available/mentorios-site2`.

Сначала сохранить резервную копию текущей серверной версии. Затем опубликовать файлы без массового удаления содержимого каталога, проверить Nginx и только после этого обновить `main`:

```bash
ssh new-server 'tar -czf /root/backups/mentorios-site-before-YYYYMMDD-HHMMSS.tar.gz -C /var/www mentorios-site'
rsync -az --delay-updates assets/ new-server:/var/www/mentorios-site/assets/
rsync -az --delay-updates styles.min.css home.min.css main.min.js site-refresh.css new-server:/var/www/mentorios-site/
rsync -az --delay-updates *.html robots.txt sitemap.xml llms.txt new-server:/var/www/mentorios-site/
git add -A
git commit -m "Describe the site update"
git push origin HEAD:main
```

После публикации проверить `https://mentorios.tech/`, чистые адреса, перенаправления со старых `.html`-адресов и файл подтверждения Яндекса. Вложения с компьютера могут иметь права `600`: перед выгрузкой публичным изображениям нужны права `644`, иначе Nginx ответит `403`. Проверять по HTTPS не только HTML, но и новые изображения, CSS, JS; сравнивать контрольные суммы с проверенной сборкой.

Конфигурацию Nginx переносить и перезагружать только при реальном изменении маршрутов или серверных настроек. Для добавления `/portfolio` изменений Nginx не требуется. Не копировать весь рабочий каталог: в нём теперь есть зависимости и инструменты сборки, не предназначенные для публикации.

## Правило документации

При каждом изменении сайта в этом же коммите актуализировать README, если поменялись страницы, контакты, версии ресурсов, аналитика, SEO, структура, запуск или публикация. Устаревшие инструкции и ссылки не оставлять.
